import { IsRedCardPipe } from './is-red-card.pipe';

describe('IsRedCardPipe', () => {
  it('create an instance', () => {
    const pipe = new IsRedCardPipe();
    expect(pipe).toBeTruthy();
  });
});
