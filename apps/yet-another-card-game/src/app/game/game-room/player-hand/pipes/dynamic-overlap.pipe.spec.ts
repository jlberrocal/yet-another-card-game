import { DynamicOverlapPipe } from './dynamic-overlap.pipe';

describe('DynamicOverlapPipe', () => {
  it('create an instance', () => {
    const pipe = new DynamicOverlapPipe();
    expect(pipe).toBeTruthy();
  });
});
