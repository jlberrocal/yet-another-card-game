import { ErrorHandlerDirective } from './error-handler.directive';

describe('ErrorHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
